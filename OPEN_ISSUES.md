# OPEN_ISSUES(升级方案执行中如实登记, 禁止占位数据冒充)

更新: 2026-07-15

## 环境/依赖
1. **HELIOS++ 未部署** — 当前节点无 `helios` 可执行文件, 无 conda-forge 安装尝试权限验证。
   按方案 Gate 0 启用 **FallbackSimulator 降级路径**(Open3D RaycastingScene 球面扫描,
   first-hit 遮挡 + 量程门 + 高斯测距噪声)。论文/报告须如实说明主仿真器为降级实现,
   不含波形/大气模型。1.2.1 的"Fallback 与 HELIOS++ 一致性测试(ρ>0.9)"**无法执行**,
   待 HELIOS++ 部署后补做。
2. **ortools 9.15 与 open3d 0.19 同进程段错误**(abseil/protobuf 符号冲突, 本机实测)。
   已锁定 `ortools==9.10.4067`(实测可共存)。升级 ortools 前必须重新验证共存性。
3. pip 安装 ortools 时曾把 numpy 拉到 2.5.1, 破坏 base 环境 scipy/open3d ABI;
   已回退 numpy 1.26.4。本仓库依赖安装务必带 `--no-deps` 检查或事后核验 numpy 版本。

## 数据(1.3/1.4/4.5 节行动清单状态)
4. **WHU-TLS**: 需在 3s.whu.edu.cn 提交 Data Request(非商用研究), 网页表单需人工填写
   与邮箱确认 —— **需要用户本人操作**, Agent 无法代为申请。申请周期未知。
5. **ETH ASL registration datasets**: 可直接下载(免申请), 本次未下载(会话时间预算
   优先用于代码与仿真实验)。转换脚本入口预留: `patent_gap/io/station_npz.py` 统一Schema。
6. **EDF/ENS challenge、Industrial3D**: 发布/注册状态未核实(需外网交互验证)。
   按方案不写入论文实验计划必选项。
7. **合作方真实变电站数据**: 1.4 字段级需求单待随合作协议发出(需用户操作)。

## 参数【推断/待核实】(pilot 后需标定)
8. 110 kV 安全距离 1.5 m —— 依据 GB 26860 类规范, **执行前以合作方规程核对**;
   代码中为 per-class 可配置(`CLASS_TABLE` / exp2 yaml `d_safe_110kv`)。
9. ρ_0=400 点/m²、O_min=0.30、组件尺寸表为可配置初值, 未经合作方标定。
10. E2 pilot 用仿真角步长 0.4°(非 0.02–0.08° 真实 TLS 档), 为控制单站射线数;
    密度模型与仿真使用同一 Θ(公式30 持久化), 物理自洽, 但绝对密度低于真实 TLS,
    ρ_req 的满足率(dens_ok)因此系统性偏低 —— 解释结果时注意。

## 方案条目与实现的偏差(如实声明)
11. 公式(9)(10)点-面关联: 仿真流水线用射线命中三角面→Patch 的直接关联
    (hit_patch 通道), 未复用母专利最近邻关联实现 —— 物理上更精确, 但与真实数据
    路径(无 hit 真值)不同, E3 真实数据实验时须切回公式(9)(10)。
12. B10 的"cost_v = 到当前站集最近站 A* 距离"实现为"到当前位姿 v_0 的 A* 距离";
    t_scan 时间当量并入预算约束而非 cost。
13. 真值 C_gt 用 S_full 站集的可见性掩码(公式27 含量程门)构造, 未对 S_full 全部
    站做点云仿真(计算量); ρ_gt 未构造, 密度指标以 ρ_req 为参照。
14. E2 pilot 方法子集 = {B0, B1, B5, B10}; B2/B3/B6–B9 基线未实现(全量 E2 前补)。
15. 母专利公式(21)(22)候选生成沿用现有 `generate_candidates` 并叠加可通行投影;
    候选距离档改为 (4, 8, 14) m 以适配户外站(母专利默认为室内 1.5–4 m)。
16. **公式(37)重叠率实现精化**: 原文二值集合 L_v^ovl(C≥τ_cov)加权在早期覆盖低时
    把所有面向缺口的候选判为零重叠 → 公式(41)硬约束整批剔除 → B10 退化为原地重扫
    (pilot 实测 awc≈0)。改为连续覆盖率 C_j 加权: O=Σ A·q·C / Σ A·q(平滑化);
    L_v^ovl 二值集合仍用于公式(38)Fisher 锚定点。另加软回退: 硬约束全灭时去门重评。
    公式(42)成本项补上方案 3.6 的 t_scan·v_move 扫描当量(否则比值贪心偏好近站)。
17. **B10 闭环调试中发现并修复的三个实现级问题**(2026-07-15, 单场景验证 awc
    0.06→0.98): (a) 候选目标按 G_task·A 排序+构件级去重(每构件≤2)+补充可通行
    网格节点候选 —— 原 top-30 G_gap 目标 73% 是"S_full 也不可见"的永久遮挡面,
    候选池坍缩到单一簇; (b) 剔除距已执行站 3 m 内的候选 —— 预测增益对确定性重扫
    不清零, TSP 把零距离候选当最便宜首站导致无限原地重扫; (c) 公式(42)的 gains
    限制到 G_gap≥τ_gap 的缺口 Patch(与公式(41) L^new 一致) —— 否则大面积中等
    分数表面稀释集合函数。(a)(c) 为方案未明说的实现级决策, 论文中须声明。
18. **ρ_0 pilot 标定**: 仿真步长 0.4° 下 10 m 处物理上限 ~205 点/m², ρ_0=400 使
    D_rng 恒饱和(全部表面永远"缺密度")。按方案"ρ_0 pilot 标定"条款取 ρ_0=50
    (≈r_opt 处可达密度的 1/4)。真实 TLS 分辨率(0.02–0.08°)下应回到 300–400。

## 2026-08-08 一轮独立审查后的修订与新增条目
19. **场景生成曾随进程而变**(已修 `ca004d0`): `generate_scene` 用内置 `hash(str)`
    播种, 而 Python 对字符串的哈希每进程随机化(PYTHONHASHSEED), 同一
    (seed, family, density) 在不同进程得到完全不同的场景。此前所有跨进程对比
    (含 E2 pilot 与首轮执行策略 A/B)的"配对"实际未配对, 结论作废; 已改用 sha1
    稳定摘要并加跨进程复现测试。
20. **公式(33) 与 (41) 的重要度精确抵消**(已修 `8101685`): (41) 收益权含
    G_task ∝ (1+E_i), (35) 的 g^rng 除以 ρ_req = ρ_0(1+λ_E·E_i)。两处取同一
    系数时, 在未饱和区(ρ̂<ρ_req)相乘得 ρ̂/ρ_0, 重要度被整除掉。实测 15 m 外或
    斜入射处主变与杂物的单位面积权重之比恰为 1.00。价值通路改取 λ_E=0(λ_E 本
    就是(33)的自由参数), 证据通路(34)仍用 ρ_req=ρ_0(1+E)。**待 A/B 确认**:
    `scripts/run_lambda_e_ab.py`, 种子 10–14, 主指标 crit_recall。
21. **规划几何与评测几何此前同源**(已修 `d72c972`): 规划器与评测器共用同一个
    `OcclusionOracle`, 公式(27)(28) 的 Vis 不是预测而就是它事后被对照的真值,
    预测误差恒为 0, "遮挡感知有效"在该设置下无法被证伪。现区分设计 BIM 与竣工
    实景: `generate_scene(n_temp=k)` 放 k 个 BIM 查不到的临时占位物, 规划器只对
    BIM 求交。n_temp=0 时两者逐比特相同, 既有基准不受影响。E3
    (`scripts/run_e3_bim_drift.py`) 扫描该参数。
22. **σ_r 对任何指标都没有影响**: `FallbackSimulator` 按 σ_r 给测距加噪并生成
    `scan.points`, 但全部指标走 `station_sample_masks` 的精确几何求交,
    `scan.points` 除 IO 校验外无人读取。因此"测距噪声"目前是未接入的参数, 报告
    中不得声称结果对测距噪声鲁棒。
23. **awc 由少数大平面主导**(已缓解 `6ab2b60`): 面积×(1+E) 加权下, S/low 场景
    4 个主变面 + 5 个建筑面占 92% 权重, 十余个断路器/互感器合计不足 4%。新增
    构件等权的 `asset_recovery`。二者分歧显著: held-out M/mid 上 B1 面积权 0.617
    / 构件等权 0.178。既有 E2 结果表中无该列(指标晚于运行加入)。
24. **配对置换检验的分辨率下限**: 9 对样本时最小可能 p = 2/2⁹ = 0.0039,
    E2 的 16 个检验经 Holm 校正后地板抬到 0.0625 —— 结构上不可能显著, 与效应
    大小无关。种子已扩到 0–7(24 对)。报告任何"不显著"时必须同时给出该下限。
25. **配准模块(36)–(40) 的数值动态范围极小**: 单场景 403 个候选上 Degen 均值
    0.9626 (std 0.0076)、R^reg std 0.0076, 且 O^reg 全部 ≥ O_min(该轮硬约束
    未剔除任何候选)。同时仿真器根本不做配准, 没有配准误差可供预测。在有真实
    配准环节的实验之前, 不得声称(36)–(40)带来了增益。
26. **无信息对照 Bdisp**(新增 `65b9bbb`): 只在可通行自由格中取"离已执行站最远",
    不读任何缺口证据/可见性/密度模型。它并不弱 —— E2 上 awc 0.896, 高于母专利
    方法 B1 的 0.737, 接近 B5 的 0.917。任何关于公式(26)–(45)增益的主张都必须
    先越过这条线。

## 2026-08-08 第二轮独立审查（代码正确性 + 统计）后的条目
27. **配准失败导致同点死锁**（已修 `4db3ff6`）: 失败站从 `obs.scans` 弹出后不再
    进入候选去重集, 而机器人已在该点(A* 距离 0、成本全局最低), 比值贪心必然重选。
    实测 S/low seed 0 六轮全停在同一点、awc=0.000。失败站位现与已执行站一同进入
    去重集(Bdisp 同理)。修前 E4 必然产出垃圾。
28. **`registration_realism` 的 `n_overlap` 用的是 Patch 采样点数而非实测点数**
    (未修): 公式(26) 采样数 `clip(ceil(A/0.01), 4, 64)`, S/low 全场 16896 个;
    实测点云约 90546 点/站。σ ≈ σ_meas/√K 因此由 `a_0`/`m_max` 两个采样常数决定,
    而非由传感器决定 —— `m_max` 64→256 会让 σ 减半。**E4 在此修正前扫 `reg_tol_m`
    实际扫的是采样分辨率。** 另: `realism.py` 的式子是受 CRLB 启发的量级估计,
    不是 CRLB(它把平移块与旋转块的归一化条件数合成了一个"米")。
29. **遮挡发现把单向高估换成了双向误差**: S/low seed 0, n_temp=6, 9 个视点 ×
    526 patch —— 仅 BIM 时高估 557 条/低估 0 条/MAE 0.0848; 并入发现体素后高估 2
    条但低估 309 条(6.53%)/MAE 0.0126。净收益为正, 但其中 20 条把真实可见率>0.5
    的目标预测成全遮, 成因是 `VOXEL=0.40 m` 外扩。另 `Z_MIN=0.35` 在障碍物底部
    留缝。**判据 `EPS_UNMODELED=0.25` 在本仿真里无法被检验**(规划几何与竣工几何
    同一批三角面, n_temp=0 时回波到 BIM 的距离 max 仅 0.0233 m), 用到真实数据前
    必须按该项目竣工偏差统计重新标定。
30. **`awc_per_1000s` 与 B10 的内部目标同构, 不能作独立确认**: B10 的
    `cost = dist + t_scan·v_move = dist + 90`, 而 `T = 2·(路径 + 站数·90) = 2·Σcost`。
    B10 用的正是边际增益/成本比值贪心, 即"每秒信息增益最大"; 其余方法目标里没有
    时间项。E2 中 B10 vs Bdisp 的预注册主指标 awc 未过 Holm(p=0.0564), 过线的是
    这个同构指标。结论须按此表述。
31. **E2 的 provenance 混杂**: 120 个 run.json 分两批 —— 种子 0–2 打
    `77e257c-dirty`/`cbfd14f224`, 种子 3–7 打 `65b9bbb-dirty`/`b0263d9f95`;
    `summary.md` 表头只记后者。两批都是 `-dirty`, 无法验证代码一致。
32. **E3 全量跑的是不含 B11 的旧代码**: 进程启动于 `8d90756`, 之后的
    `6ab2b60`(asset_recovery)/`2768698`(discovery)/`96901de`(B11 进 E3) 均未生效;
    167 个 run.json 无一含 `asset_recovery`。该批结果**能答 H1/H2/H3(λ 默认在
    `8d90756` 即为 1.0, 与 HEAD 一致), 答不了 H4**。H4 另跑 `results/exp3b`,
    禁止在混版本目录上续跑。
33. **B11 的首个单场景结论曾被自己高估 38 倍**(已更正): 那批数字测于
    `lambda_e_value=0.0`, 而该默认值当天即因 A/B 不支持退回 1.0。λ=0.0 只压低
    B10(n_temp=6 时 awc 0.9826→0.9021), B11 在两种取值下逐位相同。HEAD 上真实
    差异为 awc +0.0022 / 构件等权 +0.018。**教训: 改了默认值之后测的对照, 必须
    在默认值最终确定之后重测。**
34. **`asset_recovery` 的"构件"是建模图元不是设备台数**: `element_guid` 取自
    `_Builder.add()` 的递增 `comp_id`, 一台主变 = 箱体 + 4~6 片散热片 = 5~7 个
    "构件"。真正的设备级等权需要设备 GUID, 合成场景尚未建模。
35. **`build_trav_grid` 不看 `in_bim`**: 临时占位物照样进可通行图, 即规划器在
    导航通道上知道这些障碍、只在遮挡通道上不知道。可辩护(机器人有局部避障), 但
    这是"规划器只有 BIM"叙述的一个未声明例外, 且会让 A* 距离随 n_temp 变化。

## 2026-08-08 第三轮独立审查（全量代码 + 结论可信度 + 专利可写性）
36. **公式(41) 三项量纲失配**（已修 `3b88879`）: info 是广延量而 Ā_v 是均值、
    O_v 是比值, 配准项占 info 的比例 S/low 0.64% → M/mid 0.07% → L/high 0.02%,
    公式(41) 在大场景退化成纯 info。改用步内 info 最大值归一后为 0.8/1.0/0.7%。
    **但端到端效果未验证**: M/mid seed0 上归一前后路径同为 220 m、crit
    0.9571→0.9559。只有中间量有数据, 不得作为技术效果主张。
37. **`exec_policy` 默认值曾违背本项目自己的纪律**（已修 `c75d37a`）: 唯一的
    A/B(种子 10–14, 45 次运行)在每项上都反对 j_step —— tsp_first awc 0.977 vs
    0.967、crit 0.956 vs 0.924、路径 153 vs 230 m、ig_per_m p_holm=0.0006。
    代码注释援引的 `docs/闭环执行策略升级.md` 在 git 全历史中从不存在, 其引用的
    "边际增益 1/5～1/2"采自场景哈希缺陷修复前、已作废的诊断。默认已退回 tsp_first
    (亦即公式(45) 原定做法)。
38. **缺纯 BIM 离线覆盖规划基线**（已补 `c75d37a`, `Bbim_offline`）: 这是工业 TLS
    站位规划的现有技术主流, 也是实审最可能的对比文件类型。它很强 —— M/mid seed0
    构件等权 0.983 **高于** B10 的 0.969; B10 赢 awc(0.995 vs 0.896)与路径
    (167 vs 292 m)。缺这条线, "优于纯 BIM 离线规划"的技术效果站不住。
39. **消融链是断的**: OPEN_ISSUES #14 登记的 B2/B3/B6–B9 至今全未实现。现有
    B1 → B5(+遮挡+量程) → B10(+配准+集合选择+TSP) 中间三项无法拆开, B10 vs B5
    的差值说不清来自配准项、集合选择还是 TSP。
40. **检验族缩减 + crit_recall 出族**: 见 docs §3.1。族由 16 缩到 8 的决定由旧
    数据(awc p=0.0564)触发, 属花园分叉路径; crit_recall 移出后"关键设备召回大幅
    提升"不再有族校正后的显著性支持。E5 预算规则改动的触发原因同样是不利结果
    (6 站下 Bdisp 反超), 且 9.3 是反推得到的, 一并声明。
41. **E5 的分辨率地板未登记**: 每个 family 只有 3 密度 × 3 种子 = 9 对, 走穷举
    路径最小 p = 2/2⁹ = 0.0039, 6 个检验 Holm 后地板 0.0234 —— 要求 9 对差值符号
    完全一致才可能显著。归档 E5 里 M family 的 p=0.2500 就是 3 对样本的地板。
42. **`docs/experiment_protocol.md` 与实际不符**: 声明的种子分割是"调参 0,1,2 /
    验证 10 / 测试 100–104", 而 E2 用 0–7、E3b 用 0–4、E5 用 0–2。除 λ_E 的 A/B
    用了 10–14 外, **所有设计决策(候选集容量、预算规则、exec_policy、λ_reg、
    a0/m_max)都是在评测种子上做的**。该文件须作废或重写。
43. **对 #23 #28 #31 的更正**: #23 应为 **3** 个主变面 + 5 个建筑面, 且 92% 这个
    集中度**限定在缺口集上**(按全部 526 个分块算前 9 个只占 33.4%); #28 的"m_max
    64→256 令 σ 减半"方向与实际改动相反 —— 改成 a0=0.25/m_max=1024 后 S/low 总
    采样点由 16896 降到 8488; #31 对新批不成立, 源码工作树是干净的, `-dirty` 来自
    `results/` 目录的增删。
44. **`config_hash` 不含代码版本**: 它只哈希 config 字典, 于是改动前后两批 E2
    可以拿到同一个 `config_hash=b0263d9f95` 而 commit 相差三次算法改动。应改为
    记录 `git rev-parse HEAD` + `git diff --stat -- src` 的哈希。
45. **E2 的有效区分度可能远小于名义样本量**: S/low 与 S/mid 上 B5/B10 都已在
    0.98–0.99 的天花板, 真正有区分度的只剩 M/mid 一个场景 × 8 个种子。名义 24 对,
    有效可能只有 8 对。应把 M/high 或 L/mid 换进 E2 场景集。

## 2026-08-08 第四轮独立审查（全量代码正确性）
46. **续跑逻辑拼出版本混合结果集**（已修 `d8a8d64`, 第三次同类事故）: 三个脚本
    的断点续跑只判 `run_path.exists()`, 不校验 `git_commit`。22:24 那批跑
    `exec_policy=j_step`, 22:52 重启的跑 `tsp_first`, 旧 run.json 被全部跳过并
    保留 —— exp2 里 39 个 a22e680 + 31 个 c75d37a, `B10_full` 自己就是 7+4,
    而配对检验按 (method, scene, seed) 配, 会把两版本配到一起。`config_hash`
    两批相同(见 #44)。已加 commit 校验, 混版本目录已删除重跑。
47. **距离核默认路径回归**（已修 `d8a8d64`）: 把 σ 绑成 0.4·r_opt 后, 全仓只有
    E2 的 B1 传 `r_opt_m`, 其余 8 处(webapp/cli/run_real_pipeline/
    run_synthetic_pipeline/v1 闭环/greedy_view_set/两个测试)落到 2.5 → σ 由 2.0
    变 1.0, d=6 m 权重掉 99%、d=8 m 掉 99.999%, 有效半径从约 ±4 m 缩到 ±2 m。
    **真实交付走的正是这几条通路。** σ 缺省已恢复 2.0。
48. **对 #36 归因的更正**: 配准项占比低**不是**"info 是广延量被规模稀释", 而是
    `1−Degen ≈ 0.016`(Degen 中位 0.9842)。归一化后 `λ_reg·R^reg ≤ 0.0099` 对
    `info ≤ 1.0`, 仍只占 1%, 六轮闭环中**一次都没有改变过 top-1 候选**。真正
    该动的是 Degen 的定义或 γ, 不是 info 的归一基准。**(36)–(40) 目前对决策
    零影响, 不得主张任何效果。**
49. **归一化把 η·O_v 的有效权重放大约 58 倍, 并改了 B5 的行为**（未修）:
    S/low 首轮 `info_scale=58.33`, 新式 info 中位 0.088 < 惩罚上限 0.1, 于是
    564 个候选里 224 个 value ≤ 0; 而 B5 用 `value > 0` 作硬门槛, 预算紧时会比
    改动前更早终止。**B5 是预注册检验的基线**, 基线行为被无意改动会直接影响
    B10 vs B5 的结论。η 需随 info 一起重新标定。
50. **采样常数把两个指标的尺子推向相反方向**（未修）: a0 0.01→0.25 让 awc(面积
    加权)的密度近似恒定(3.11/5.05/9.53 → 4.02/4.08/4.35 点/m²), 但 E≥0.8 的
    关键 patch 面积中位仅 0.24–0.26 m², 采样点数由 26 降到 **4**(S/low 84.7%、
    L/high 98.1% 的关键 patch 只有 4 点), 于是 coverage 只能取 {0,.25,.5,.75,1},
    而 crit_recall 判据是 `C_now ≥ 0.5·C_gt`。**asset_recovery / crit_recall 的
    分辨率被砍到 4 个点**, 而 asset_recovery 正是为修 #23 才加的。另: `sampling.py`
    注释里引的旧密度"越大越稀 −25%"复现不出(按面积加权是越大越密 +206%), 该依据
    不成立。
51. **`register_station` 的随机分支实践中从不触发**（未修）: 现实取值下
    (n≥200, degen≤0.99, σ_meas=0.01) σ≤0.0072 m, `P(|N(0,σ)|>0.05) < 3e-12`;
    实测 degen 中位 0.9842。即它等价于确定性的 `overlap ≥ o_min`, CRLB 那一段
    是死的, **E4 扫 `reg_tol_m` 会得到一条平线**。
52. **测试质量问题**: `test_registration_realism` 首条断言是把公式和它自己比
    (σ ∝ 1/√K, K 差 100 倍必然 σ 差 10 倍, 恒真); `test_every_component_can_be_
    targeted` 把 `_make_candidates` 前五行原样抄进测试而不调用它; `test_b11_
    equals_b10` 只锁 n_temp=0 的平凡分支, 从不验证 n_temp>0 时 B11 确实不同;
    `test_discovers_the_occluders` 的 ±0.40 m 容差刚好宽到测不出 #29 的外扩;
    `test_tight_budget` 把一次观测锁成"awc 随预算单调"的不变量, 贪心无此保证;
    `test_every_scene_family_runs` 打了 slow 标记, 默认档全部跳过。
53. **`n_stations` 含配准失败被丢弃的站**: 作为时间成本正确, 作为"站数"列会被
    误读。`n_targets = clip(2·n_comp, 60, 400)` 在构件数 > 200 时又会截断尾部,
    重新触发 #38 修掉的缺陷(合成场景最大 144, 真实 110 kV 站会超)。
54. **审查 A 穷举验证通过的部分**（可放心）: `distance_field` 与 `astar` 在
    24330 个格点对上可达性零不一致、最大数值差 4.44e-16, 五种边界均一致;
    `ranking_v2` 两趟改写的打包/解包逐位对应、`out.sort` 稳定、`info_scale=0`
    时正确退化; **子模性未被破坏**(`lazy_greedy` 吃的是未归一的 `q_row` 与
    `gains`, 从不碰 `value`); patch 顺序三套数组同序; 确定性无残留(无内置
    `hash(`、无未播种 RNG); `n_temp=0` 与 `bay_pitch` 的向后兼容成立;
    退化预算(length/stations/rounds 为 0)不崩。
