# 07 专利技术方案修改影响分析（Phase 7）

**本文件给出修改建议，不是最终申请稿。** 有益效果一节的具体数字待 E7/E8 定稿后
填入（见第七节）。

对照本：`送审_补充扫描规划专利/01_申请文件/`（权利要求书、说明书）。

---

## 一、发明名称

现名：**一种基于设计模型与在线遮挡发现的补充扫描规划方法及系统**

问题：「基于设计模型」在字面上把整个方案系上了「有一份描述现场的设计模型」这个前提，
正是合作方质疑的落点。

建议改为：

> **一种基于目标参考模型与在线环境更新的补充扫描规划方法及系统**

理由：把「设计模型」换成「目标参考模型」，明确它只承担「要检查什么」；把「在线遮挡
发现」上位为「在线环境更新」，涵盖遮挡侧与占据侧两条回灌。**[C]**

不建议改成「未知环境」「自主探索」一类措辞 —— 本方法**不是**无模型建图，目标参考
模型是必需的（见 §12 禁止项 1、2、3）。

---

## 二、术语：一个词拆成两个

现稿全篇使用「设计建筑信息模型」一个词，同时承担两种互不相同的职责。建议在说明书
「术语」处引入并全篇贯彻：

| 新术语 | 定义 | 现稿中对应处 |
|---|---|---|
| **目标参考模型** | 描述待检查目标构件、目标表面及其工程属性；不要求描述现场全部环境几何 | S1、S4 的「表面」、S7 的比对基准 |
| **规划环境模型** | 描述规划器当前认为现场哪里被占据、什么挡住视线 | S2 的可通行空间 ＋ S4 的「规划用遮挡模型」 |

**规划环境模型含两个子部分，其信息来源不同，说明书须分开写清 [A]：**

| 子部分 | 初始内容来自 | 更新方式 |
|---|---|---|
| **通行占据侧** | 已采集点云（＋可信安全禁入约束） | 每轮以新增点云按同一规则并入 |
| **可见性遮挡侧** | **至少包含目标参考模型的目标表面**，及本实施例固定保留的基础几何（地面）；可另含已知的部分环境几何 | 每轮并入 S7 判定所得的占据几何 |

> **不得写成「规划环境模型完全由点云建立」** —— 该表述对遮挡侧不成立。
> 推荐统一表述：**规划环境模型的通行占据状态主要由实际观测建立并逐轮更新；用于
> 可见性求交的遮挡表示至少包含目标参考模型中的目标表面，并可结合已知环境先验及
> 新增观测进行更新。**

一条必须写进说明书的一致性约束：

> 凡目标参考模型声明为目标的表面，规划环境模型**遮挡侧**的初始内容必须包含其几何。

否则会出现「要求扫一个拿不到几何的面、并在求交时以为能透视过去」的矛盾。**[A]**

---

## 三、背景技术与技术问题

### 3.1 背景技术：增补一段

现稿背景技术围绕「设计模型中不存在的物体导致可见性被单向高估」。建议增补：

> 在部分竣工测量任务中，设计模型主要对**待检查的目标构件**作了完整描述，而对现场的
> 临时设施、堆放物、施工机具乃至部分永久性非目标构筑物，可能描述不全、与实际不符或
> 未予记录。部分基于模型的站位规划方式要求由该模型直接生成移动平台的可通行空间与
> 视线遮挡模型，因而隐含要求该模型完整描述现场环境几何。

> **[D／待代理人检索确认]** 上段中"通常""普遍"一类的领域事实判断，**本项目实验并未
> 直接证明**。已按保守措辞写成"部分……""可能……"。是否可以改回"通常""普遍"，取决于
> scan-to-BIM / NBV / inspection planning 领域的现有技术检索结果，须由代理人确认。
> 详见第八节。

### 3.2 技术问题：由两个增为三个

现稿两个问题：(一) 可见性被系统性单向高估且逐轮重复；(二) 多站补充扫描在预算收紧时
提前终止并输出零结果。

建议增列第三个，并置于首位：

> **问题一**：既有方法要求由设计模型直接生成移动平台的可通行空间与视线遮挡模型，
> 从而要求该模型完整描述现场环境几何。现场不具备该条件时，规划所依据的可通行空间
> 与遮挡关系自始即为错误，且该错误在整个补充扫描过程中得不到纠正。

原两个问题顺延为问题二、问题三。

---

## 四、技术方案：S1–S8 的逐条修改

### S1 — 术语 ＋ 处理对象口径

「设计建筑信息模型」→「目标参考模型」。

**分块、点—分块关联、缺口证据、G_gap、G_task 的算法与公式未作任何改动 [A]；但其
处理对象已明确限定为目标参考模型中声明的目标构件/目标表面 —— 非目标环境构件
（厂房、围墙、杂物）不再进入待补扫描目标集 [A]。** 该口径统一是本轮的实质改动之一，
不是纯术语替换。

### S2 — 实质修改（本轮的核心）

**现稿**：

> S2，依据所述设计建筑信息模型中构件的平面占位与高度信息，以及移动平台的尺寸，
> 构建所述移动平台的可通行空间；

**建议改为**：

> S2，构建所述移动平台的规划环境模型；所述规划环境模型将平面区域划分为已确认占据、
> 已确认自由与未确认三种状态；其初始内容至少依据所述已采集点云确定：将所述已采集
> 点云中高度处于所述移动平台通行高度区间内的回波点所在区域确定为已确认占据，将
> 各采集站位至其各回波点之间连线所经区域确定为已确认自由，其余区域为未确认；
> **所述未确认区域不作为可通行区域**；并依据所述移动平台的尺寸对已确认自由区域作
> 收缩，得到可通行空间；

要点，逐条都对应代码中的一处 **[A]**：

1. **三态**，而不是「障碍/非障碍」二态。未确认必须是独立状态。
2. **未确认不得默认可通行**。这是弱先验下最容易偷跑的一处，须写进权利要求正文而非
   仅在说明书提及。
3. **自由区来自射线穿越**，不是来自「模型里此处为空」。
   > 说明书须同时写明其**实施方式假设**：当前实施例按二维栅格实施，将射线穿越区域
   > 作为自由空间的观测证据；该状态是**基于二维投影的规划近似**，最终仍受平台半径
   > 收缩、已确认占据优先与安全禁入区约束。**不得写成"证明该位置可通行"** **[A]**。
4. 初始内容来自**已采集点云**，即产生了待补缺口的那次初始扫描，不需要额外作业。

### S3 — 不变

候选生成读目标参考模型的分块位置与法向、读 S2 的可通行空间，随 S2 自动跟随。**[A]**

### S4 — 改一句

**现稿**：「所述规划用遮挡模型的初始内容为所述设计建筑信息模型的表面」

**建议**：「所述规划环境模型遮挡侧的初始内容**至少包括**所述目标参考模型的目标表面」

需要展开时可加：「所述遮挡侧的初始内容还可以包括预先可获得的基础环境几何或部分
已知环境几何。」

> **"至少包括"三个字不能省。** 真实实现中该初始内容还含地面，以及该场景下可获得的
> 部分已知环境几何；写成"为目标表面"比实现更窄，会与 S7 的比对基准自相矛盾。**[A]**

解释应写为：**规划器至少掌握待检查目标的参考几何，但不要求目标参考模型预先完整
描述全部非目标现场环境。** 不得写成"规划器一开始只知道目标长什么样，不知道现场还有
什么"。

### S7 — 改比对基准，判据本身不动

**现稿**：「计算所述新增点云中各回波点至所述设计建筑信息模型**全部表面**的最近距离」

**建议**：「计算所述新增点云中各回波点至所述规划环境模型遮挡侧**初始内容**的最近
距离」

判据 `d(x, T) > ε` 一字未改，只换 T。**T 必须写成"遮挡侧初始内容"而不是"目标表面"**
—— 代码中的基准是 `world.ref_tris`，即**该先验档实际给出的全部几何**（目标表面 ＋
地面 ＋ 当时保留的已知环境构件），不是仅目标表面 **[A]**。写成"仅目标表面"会与实现
不符，且会让已作为先验给出的环境构件在文字上被重复计入"新发现"。

语义随之由「模型外临时异物」上位为「**规划环境模型遮挡侧初始内容所未描述的现场
占据几何**」。已作为先验存在的环境构件**不会**被重复发现（实测 P100 档只发现设计
模型中不存在的临时占位物），见 `09_剩余代码逻辑核查_FINAL.md` 核查一。**[A]**

### S8 — 增加一条回灌

**现稿**：S8 把体素并入规划用遮挡模型，并更新缺口证据、任务分数、缺口表面分块。

**建议增补**：

> …并以所述新增点云按 S2 所述方式更新所述规划环境模型的所述三种状态；

即闭环二的另一半：新增观测不仅修正**看得见什么**（回灌 S4），也修正**走得到哪里**
（回灌 S2）。现稿只有前一条，`world.grid` 在整个闭环中从不更新。**[A]**

---

## 五、权利要求 1

改动集中在 S2、S4、S7、S8 四处，措辞见第四节。三点须一并注意：

1. **S2 的改写是上位化还是限缩？** 现稿 S2 要求「依据设计模型构件的平面占位与高度
   信息」构建可通行空间；新 S2 要求「依据已采集点云」。二者是**不同的技术手段**，
   不是包含关系。若希望同时覆盖两种来源，须把 S2 写成
   「依据已采集点云、或依据已采集点云与目标参考模型两者」，并把纯观测式写入从属权。
   **建议采用纯观测式作为独立权** —— 它才是本轮要主张的区别技术特征；由设计模型
   生成可通行空间属于现有技术做法，写进独立权反而削弱新颖性论证。**[C]**
2. **「未确认区域不作为可通行区域」必须在独立权正文中。** 它是本方案与「模型里为空
   即可走」的分界线，放进从属权等于把区别特征让出去。**[C]**
3. **引用基础**：S2 新引入「已确认占据/已确认自由/未确认」「通行高度区间」三个概念，
   S8 回引 S2 时须写「按 S2 所述方式」，不得另起措辞。

---

## 六、从属权

| 权项 | 现状 | 建议 |
|---|---|---|
| 7 | S2 的二维栅格实施细节（由模型构件标记障碍、膨胀） | **改写**：改为三态栅格的实施细节（高度区间、射线穿越标记自由、占据不被后续射线抹回自由、按半径收缩） |
| — | 无 | **新增**：可信安全先验 —— 带电体禁入区等作业安全规程所确定的禁入区域，在无观测时即写入规划环境模型，且只增加禁入、不增加可通行 |
| — | 无 | **新增**：目标参考模型的一致性约束 —— 规划环境模型的初始遮挡内容包含目标参考模型的全部目标表面 |
| — | 无 | **可选新增**：当满足剩余预算的候选站位为空时，自已确认自由区域与未确认区域的边界邻域中选取本轮执行站位（弱先验下候选池枯竭的兜底；**是否纳入取决于 E7 的终止原因分布**，见第七节） |
| 8 | 单源最短路 | 不变 |
| 9、10 | 配准可用性判定 | 不变 |
| 2–6 | 执行规则与预算回退 | 不变 |
| 11 | 系统权 | 随 S2 模块名调整为「规划环境模型构建与更新模块」 |
| 12 | 介质权 | 不变 |

---

## 七、有益效果

数据来源：E7（`results/exp7/`，240 次运行）、E8（`results/exp8/`，144 次运行），
均产自 commit `5116dae`，全部 `status=ok`。检验为配对置换 ＋ Holm 校正，24 组配对。

建议在说明书「有益效果」处按下表分四条写，**每条各自独立，不得互相改写**：

### 效果一 —— 移动可通行空间可以整个改由观测建立，未检测到显著性能下降

把可通行空间由完整参考模型栅格化改为观测驱动（`LEGACY → P100`），面积加权恢复
−0.001、资产恢复 −0.004、单向高估 −0.001，**四项 p_holm 全部为 1.0000**。

> 可写：本方法不要求由参考模型生成移动可通行空间；改由实测点云建立并逐轮更新后，
> 在同等预算下未检测到显著的性能下降。

### 效果二 —— 参考模型不描述现场环境时仍可运行，但代价须如实写明

参考模型不提供额外非目标环境构件几何先验（P0）时，相对完整环境先验：

| | 面积加权恢复 | 资产级恢复 |
|---|---|---|
| **不开**在线环境更新 | −0.067（p_holm 0.0016） | −0.096（p_holm 0.0075） |
| **开**在线环境更新 | −0.003（p_holm 1.0000） | −0.027（p_holm 0.0464） |

240 次运行**全部正常用满轮次**，无一因候选池枯竭提前终止。

> 可写：参考模型不提供额外非目标环境构件几何先验时，本方法仍能在预算内完成全部补充扫描
> 轮次；配合在线环境更新，面积加权缺口恢复率相对完整先验条件无显著差异，资产级
> 恢复率下降 0.027。
> **不可写**：「无性能损失」——资产级的 0.027 已达显著（p=0.046）。

### 效果三 —— 在线环境更新的价值随参考模型描述环境的程度减弱而增大

`B11 − B10` 的资产恢复增益：P100 **+0.012** → P75 +0.045 → P50 +0.061 →
P0 **+0.092**（p_holm 0.0117）；面积加权 +0.010 → +0.073（p_holm 0.0030）。
可见性单向高估在**全部五档**由 0.062–0.081 降至 **0.001–0.002 量级（各档均不高于 0.002）**，p_holm 全为 0.0020。

> 可写：在线环境更新后，各先验档的可见性单向高估**均不高于 0.002**（实测 0.001–0.002）；其对最终恢复率的
> 增益随参考模型所描述环境几何的减少而增大，自 +0.012 增至 +0.092。
> **不可写**：「在线发现总能显著提升恢复率」—— P100/P75/P50 三档的资产增益
> p_holm 分别为 0.47 / 0.14 / 0.09，未达显著。

### 效果四 —— 与探索式站位选择优化的不是同一个量

同平台、同传感器、同预算、**相同站数（均 6 站）**下（P0）：

| | 探索式（frontier） | 本方法 |
|---|---|---|
| 地图覆盖 | **0.952** | 0.949 |
| 资产级恢复 | 0.523 | **0.922** |
| 路径长度 | 166.0 m | 155.5 m（Δ −10.51 m，p_holm 1.0000，**不显著**） |

> 可写：在相同站数与预算下，按任务缺口计价的站位选择所恢复的资产级覆盖较按地图
> 信息增益计价的探索式选择高 0.40（p_holm 0.0040），**且路径未见增加**。
> **不可写**：「路径更短」—— B11 与 frontier 的路径差 −10.51 m 的 p_holm 为 1.0000，
> CI [−24.47, +4.01] 跨 0，未达显著。
> **必须一并写明**：地图覆盖上探索式方法略高（0.952 对 B11 的 0.949，Δ = −0.0028，
> p_holm = **0.1300**，未达显著；若与 B10 相比则为 Δ = −0.0034、p_holm = 0.0252，达显著），
> 该项是其目标函数所在，不得省略，也不得表述为本方法的缺陷（§12 禁止项 8）。

### 一并写入的边界

- 真实建筑（CRAS）的 IFC 中**不存在非目标环境构件**（实测 256 个构件全部为目标类），
  遮挡先验强度这一轴在真实数据上退化，真实数据只能支持通行侧的结论。**[A]**
- 合成变电站中可移除的非目标环境几何按三角面计只占 **3.6–5.3%** —— 变电站里遮挡
  设备的主要是**其他设备**，而设备本身就是目标。上述 0.096 的代价是在这个比例下测得
  的，**不得外推**到非目标环境更多的场景。**[A]**
- E3b 先前「在线发现对恢复率的增益不显著（p=0.50）」的结论**保持不变**，不得改写。
  E7 说明的是二者测量的先验条件不同（E3b 为完整环境先验、15 组配对、旧目标集口径）。

---

## 八、给专利代理人：事实与策略必须分开

本文件的内容分两类，**性质完全不同，不得混读**。

### 8.1 [A] 代码 / 实验已经支持的**事实**

| 事实 | 依据 |
|---|---|
| 不提供额外非目标环境构件几何先验（P0）时流程可运行，240 次全部跑满轮次 | E7 |
| M_plan 的通行占据侧可由观测逐轮建立与更新，已确认比例 0.893 → 0.949 | E7 |
| LEGACY→P100 四项 p_holm 均为 1.0000 | E7 |
| P0 相对完整先验：不开在线更新 −0.096（p 0.0075）；开在线更新 −0.027（p 0.046） | E7 |
| 在线环境更新增益随先验减弱由 +0.012 增至 +0.092（p 0.0117） | E7 |
| 相同站数下资产恢复较 frontier 高 0.40（p 0.0040），路径更短 | E8 |
| M_ref / M_plan 的数据流与两条回灌通路 | 代码 |
| 已作为先验的环境构件不会被 S7a 重复发现 | `09` 核查一 |
| 二维 FREE carving 的误判量级 1.41%，反方向 14.31% | `09` 核查二 |

### 8.2 [C] 专利**保护范围建议**（不是"算法必须如此"）

| 建议 | 性质 |
|---|---|
| 发明名称是否改为「基于目标参考模型与在线环境更新…」 | 命名策略 |
| S2 是否把纯观测式写入独立权 | **范围取舍**，见第五节第 1 点 |
| M_ref 的哪些属性写入独立权、哪些放从属权 | 见 `08` 问题 3 的分级表 |
| 是否保留「目标表面作为初始遮挡先验」的备用权项 | 退路设计 |
| 「未确认区域不作为可通行区域」置于独立权正文 | 区别特征布局 |

> **上述建议均为 [C]，不得写成 [A]。** 例如"建议纯观测式写入独立权"是**保护范围
> 策略**，**不是**"算法必须如此" —— 代码本身两种来源都能支持。

### 8.3 需代理人现有技术检索确认的事实性表述 **[D]**

以下均为**领域事实判断**，本项目实验并未直接证明。现稿已按保守措辞写为
"部分……""可能……"，是否可以改回"通常""普遍"须经检索确认：

1. 竣工测量现场的设计模型是否"**通常**"只完整描述目标构件；
2. 既有站位规划方法是否"**普遍**"要求由模型直接生成可通行空间与遮挡模型；
3. scan-to-BIM / NBV / inspection planning 领域是否存在可支撑上述判断的现有技术；
4. 背景技术是否需要采用更保守的写法；
5. 在先申请（母专利）的申请日、公开日、法律状态，以及能否主张优先权
   （沿用《待确认事项》第 6、7 项，本轮未改变该结论）。
