# FINAL_PACKAGE_CHANGELOG

最终收口轮。**未改动任何算法逻辑，未重跑 E7/E8，未改动任何正式实验数字。**

---

## 一、本轮修改的 6 项

### 16. M_plan 技术定位总括句过度简化

**问题**：仍写「用于移动和可见性预测的规划环境模型根据当前实际观测建立并持续更新」。
该句对 **M_vis 不成立** —— 遮挡侧初始内容至少含 M_ref 目标表面与地面，并可含该档
允许提供的已知环境几何。

**改为**：「规划环境模型中的通行占据状态主要由实际观测建立并逐轮更新，用于可见性
求交的遮挡表示则至少包含目标参考模型中的目标表面，并可结合可获得的环境先验及新增
观测持续更新。」

**涉及文件**：`00C`、`08`（另 `00A`/`00B`/`02`/`07`/`10` 已在上一轮同步，本轮复查一致）。

---

### 17. `07` 中 S4 的建议措辞比真实实现窄

**问题**：写成「所述规划用遮挡模型的初始内容**为**所述目标参考模型的目标表面」，
并解释为「规划器一开始只知道目标长什么样」。真实实现的初始内容还含地面与该档保留的
部分已知环境几何。

**改为**：「所述规划环境模型遮挡侧的初始内容**至少包括**所述目标参考模型的目标表面」；
解释改为「规划器至少掌握待检查目标的参考几何，但不要求目标参考模型预先完整描述全部
非目标现场环境」。**S7 仍保持「至规划环境模型遮挡侧初始内容的最近距离」，未退回
「至目标表面」。**

**涉及文件**：`07`。

---

### 18. `04` 第 3.1 节标题与正式结果矛盾

**问题**：标题为「先验减弱确实有代价，且**只发生在不开在线更新时**」。而开在线更新后
P0 资产级恢复仍为 **Δ = −0.027、p_holm = 0.0464**，属小幅但显著下降。

**改为**：标题「先验减弱的性能代价在不开在线环境更新时最明显」；正文加入明确结论
「在线环境更新将损失大幅缩小 —— 面积加权的剩余差异未达显著（−0.003，p_holm 1.0000），
**资产级恢复仍存在 0.027 的小幅显著下降**（p_holm 0.0464）。**不得写成"代价完全
消失"。**」

**涉及文件**：`04`。

---

### 19. 测试数 125 / 128 两套口径并存

**核实**：
- E7/E8 正式结果产出版本 `5116dae`：`tests/test_weak_prior.py` **18 项**，全套 **125 passed**；
- 本轮 S7a 与 FREE-carving 核查后新增 **3 项**回归测试（`test_known_prior_environment_is_not_rediscovered`、
  `test_discovery_accumulation_is_idempotent`、`test_free_carving_is_a_two_dimensional_approximation`），
  该文件 **21 项**，全套 **128 passed**（本轮实测两次，均 128）。

**统一为分层表述**：「E7/E8 正式结果产自 commit `5116dae`；该版本当时 125 项测试全部
通过。完成最终 S7a 与 FREE-carving 核查后新增 3 项回归测试，当前最终交付测试集共
128 项，全部通过。」并注明**新增测试只锁定既有行为，未触发重跑**。

**涉及文件**：`00A`、`00C`、`03`、`08`、`10`。

---

### 20. `05` E8 主表路径一行混用 B10 / B11

**问题**：主表列头为 `Δ(B11 − frontier)`，路径一行却填 `−36.3（B10）`，属**表格列定义
错误**。

**处理**：采**方案 A** —— 主表统一只报 `Δ(B11 − frontier)`。从正式汇总
`results/exp8/summary.md` 读取原始值，未手算：

| 比较 | Δ | 95%CI | p_holm |
|---|---|---|---|
| **B11 − frontier** | **−10.5124 m** | [−24.4650, +4.0072] | **1.0000** |
| B10 − frontier | −36.2648 m | [−50.3723, −21.2043] | 0.0040 |

B10 一行另在正文单列说明。

> **本项另带出一处必须纠正的主张。** 原文把「路径更短 10 m」写成本方法的优势。
> B11 的路径差 **p_holm = 1.0000、CI 跨 0，未达显著**。已全包改为
> **「站数完全相同（均 6 站），且路径未见增加」**，不再作为优势主张；
> `07` 的「可写/不可写」栏另加一条明确禁止写「路径更短」。

**涉及文件**：`05`、`00C`、`07`、`08`。

---

### 21. 「全部五档降到 0.002」不精确

**正式数据**：B11 可见性单向高估 LEGACY 0.001 / P100 0.001 / P75 0.002 / P50 0.002 /
P0 0.002。

**改为**：「各先验档均**不高于 0.002**（实测 0.001–0.002 量级）」。

**涉及文件**：`00B`、`00C`、`04`、`07`、`08`。

---

## 二、测试最终真实数量

| | 测试数 |
|---|---|
| E7/E8 产出版本 `5116dae` | **125 passed** |
| 最终交付版本 | **128 passed** |

差额 3 项为本轮 Phase A 核查的回归锁定，只固化既有行为，**不改变任何算法路径**。

## 三、E7/E8 是否重跑

**否。** 两项代码事实核查（S7a 比较基准、二维 FREE carving）均判定为
**非逻辑错误**，未触发《重跑必要性说明》。

- E7：240 次运行，`git_commit` 单一值 `5116dae`，全部 `status=ok`；
- E8：144 次运行，同上。

## 四、是否改变任何正式实验数字

**否。** 本轮只改文字。所有 headline 数字经
`scripts/check_delivery_numbers.py` 回原始 `run.json` 重算核对，**全部通过**
（该脚本逐位复现汇总脚本的 40 项 Holm 检验族，因为族大小不同 p_holm 就不同）。

## 五、是否改变任何算法逻辑

**否。** 未触碰 S1–S8 主链、G_gap/G_task、S5 子模集合选择、预算逻辑、S7a 判据、
`PlanningEnvGrid`、frontier baseline。

---

## 六、最终状态

| 项 | 结论 |
|---|---|
| TECHNICAL LOGIC PASS | ✅ 两项代码事实核查完成，文字已与代码对齐；未发现逻辑错误 |
| EXPERIMENT CONSISTENCY PASS | ✅ 数字核查脚本全部通过；E7/E8 冻结未重跑；统计比较对象已逐处明示 |
| ORACLE LEAKAGE AUDIT PASS | ✅ `06` 第六节复审：P0 决策前不读完整障碍图、完整求交网格、临时物位置、真值可见性与真值自由空间；残留项（初始站位）已披露 |
| DOCUMENT CONSISTENT PASS | ✅ §11 全部 16 个关键词扫描清零；13 份 DOCX 无 markdown 残留、无表格列数不一致、无连续空段落；交叉引用全部可解析 |
| **READY FOR PATENT AGENT** | ✅ |

### 保持披露、未解决的适用边界（7 条，未弱化）

1. 初始站位属于作业输入，不由本发明规划；
2. P0 下仅初始三站后 M_occ 已确认约 **0.893**（0.814–0.950）——属"已有初始扫描之后的补扫"；
3. FREE carving 是二维投影近似（误判可通行 1.41%，保守漏判 14.31%）；
4. 合成变电站中非目标环境仅占三角面 **3.6–5.3%**，代价数字不得外推；
5. CRAS 真实建筑无法验证遮挡先验轴（256 个构件全部为目标类）；
6. E8 只直接实验对照 frontier-based exploration，**不是 NBV/SLAM benchmark**；
7. E7/E8 与早期 E2/E5/E6 绝对值不可跨实验比较（目标集口径已统一）。

另保留的三条不利于本方法但真实的结论：**P0 资产恢复仍有 −0.027 显著下降**；
**frontier 地图覆盖数值略高**；**在线环境更新并非在每个先验档都显著提高最终恢复率**
（P100/P75/P50 的资产增益 p_holm 分别为 0.47 / 0.14 / 0.09）。
