# 合作方权利要求 1–2 答复交付包（2026-07-31）


> **【审阅及策略确认稿，非最终定稿】** 本次交付为技术答复与权项策略请示。文件中同时保留了合并式与拆分式两套权利要求结构，二者不可并存；请代理人确认采用哪一套后，我方再统一权利要求书与说明书。
针对合作方（代理人）《权利要求1-2（基于建筑点云缺口检测的补充扫描视点规划方法）.docx》中
**S4 的两个疑问**以及"这样的描述在逻辑上是否正确"的提问，本次完成解答、逻辑审查与文件更新。

---

## 一、先看这两份

| 顺序 | 文件 | 说明 |
|---|---|---|
| ① | `02_修订版权利要求/权利要求结构两方案对照_20260731.docx` | **唯一必须由代理人拍板的事项**：合并式 vs 拆分式权利要求，含我方建议与三项待确认 |
| ② | `01_答复文件/S4疑问答复与权利要求逻辑审查_20260731.docx` | 两个疑问的完整解答 + 权利要求 1–2 的逐条逻辑审查 |
| ③ | `02_修订版权利要求/权利要求1-2_修订版_20260731.docx` | 按审查意见修改后的权利要求，附逐条对照表 |

> ⚠ **本交付包中同时存在两套权利要求结构，二者不可并存**：代理人本次所撰《权利要求1-2》为合并式（S1–S10 同在权 1），我方《权利要求书_单独版》为拆分式（权 1 只做检测）。在代理人择一之前，**均不应视为最终定稿**。我方建议采用拆分式，理由见 ① 号文件。

---

## 二、两个疑问的结论

**疑问 1（公式(9)中 x、y 的数学形式与距离计算）** —— 我方原稿确实缺漏，已补齐：给出 x 的坐标形式、y 的重心坐标形式、欧氏范数展开，以及点到三角面最近距离的**闭式求法**（投影判定 + 边/顶点回退）。该闭式算法与本项目实际实现比对，400 个随机点上**最大偏差 1.46×10⁻⁷ m**，在单精度浮点精度内一致。

**疑问 2（是否存在距离相等、应归属谁）** —— **存在，且不是可以忽略的个例。**

- 在**凸棱**（两表面夹角小于 180°，如柱棱、墙角、梁棱）外侧，以该棱为脊、张角等于 180° 减去两面夹角的**三维楔形区域**内，每一点的最近表面点都落在棱上，因而到棱两侧表面的距离**处处严格相等**——这是**正体积区域**，不是零测度的个别点（区域内的点可距棱任意远，落在棱上的是它们的最近表面点）；
- 更关键的是，双法向约束恰好会把夹角大的两个面分到**不同的表面分块**，该棱通常正是**两个表面分块的公共边界**；在距离阈值 τ_d 限制下，真正参与匹配的等距点位于距该棱 τ_d 以内；
- 我们还测出：现有实现（Open3D）在并列时的取舍**随三角面存储顺序而改变**，底层库未承诺任何规则。

**并列在真实数据上不是边角情形**：在真实 IFC 网格（1,197,750 个三角面）与 20,000 点的实测中，**43.01% 的点**会触发并列裁决（容差 1×10⁻⁶ m），其中 6,840 个匹配点（占匹配点的 67%）的表面分块归属会因三角面存储顺序不同而改变。

因此必须在方案中**显式规定唯一归属规则**。修订稿 S4 写入的是上位表述"按预设的确定性规则从中择一"；具体规则在我方说明书中完整给出：第一级取最近距离最小者，第二级取"由最近表面点指向该点的方向与该面法向所成夹角的**余弦绝对值**最大者"（取绝对值以不受顶点绕序影响；最近距离为零时该方向无定义，视为并列直接进入下一级），第三级取三角面索引最小者。另给出一个可选的 1/k 均分方案供代理人择定（采用时 S4 须同步改动，详见修订稿末尾提示）。

以上结论均可用 `04_验证材料/verify_s4_association.py` 复现（运行结果已存 `验证运行结果.txt`）。

---

## 三、逻辑审查结论

**S1→S10 的顺序与因果关系成立**，与我方技术交底、公式(1)–(25)、实施例 1–9 的次序完全对应，可作为整体技术方案的骨架。权利要求 2 的闭环表达也正确。

但有 **6 处需要修改**，其中 3 处为实质问题：

| # | 问题 | 严重度 |
|---|---|---|
| 1 | S3+S4 按字面会使一个点同时匹配多个三角面而被**重复计数**，与公式(9)(10)"每点一个距离值、一个指示值"不符，会系统性抬高覆盖比例与点密度 | 严重 |
| 2 | S5"由**指标**构造证据"与 S6"由证据得到**指标**"**术语层级颠倒**（方向反了，且五个"指标"在权项中无定义、无引用基础）；五个类别应是证据的**来源**而非指标 | 严重 |
| 3 | S7 丢失了本案最核心的发明点——重归一化须在**实际可用的**指标集合上进行，不可获得项不进分子分母（公式18、19；有益效果第4条） | 严重 |
| 4 | S10 把价值计算限制在**目标表面分块**上，而公式(25)是对候选位姿视场内**全部**被计入收益的表面分块求和；且与原权13 的细化范围不一致 | 实质 |
| 5 | S1"单个构件"把方法限缩到一个构件，与 S3"场景的三维点云"、S5–S8"各表面分块"尺度不一致 | 表述 |
| 6 | 若干措辞：三角面"分割"、两项法向约束未指明比较对象、缺"种子三角面"、权 2 多一个"在"字、"三维点云的新增点"来源表述 | 表述 |

---

## 三·五、确定性规则已落到实际实现（本轮完成）

上一版仅在说明书中规定了规则，实现尚未同步。本轮已完成，并验证四项：

| 要求 | 结果 |
|---|---|
| 规则进入实际关联程序 | ✅ 新增 `src/patent_gap/data/tie_break.py`，接入 `associate_points_to_ifc`；单测断言其确被调用 |
| 改变三角面存储顺序结果不变 | ✅ 三类等距几何（凸棱／共面相邻／不相邻平行面）枚举全部排列，加规则后归属恒定 |
| 真实点云上的影响已测量 | ✅ 触发裁决 43.01%；匹配点数 10,154→10,154 无变化；跨分块改变 6,840 点 |
| 各分块点数之和 ≤ 总点数 | ✅ 10,154 ≤ 20,000；每点仅一个归属 |

**过程中发现并修正了两个缺陷**（均在把规则写成代码真跑起来后才暴露）：
1. 规则末级"取三角面**索引**最小者"中的索引即存储顺序，实测仍给出不同归属——**该末级并不能达成它声称的目的**。已改为按三角面顶点坐标的字典序裁决。
2. 实现的候选集只取"与基准面共顶点"的三角面，**漏掉互不相邻的等距面**（如两片平行表面正中的点）——而这恰是我方自己列举过的等距例子。已改为按外接球半径分桶的**精确全局范围查询**。

说明书、答复与修订稿均已同步；单测中已加入两项回归断言。

## 四、我方文件的同步更新（`03_我方文件更新/`）

1. **说明书**：公式(9)后新增 2 段（x/y 的数学形式、点到三角面的闭式求法含退化面处理、`d(x,T)` 与最近三角面 `t*(x)` 的定义），公式(10)后新增 3 段（匹配与归属规则、等距情形的成因与三级唯一归属规则、可选的 1/k 均分方案）；主要符号说明补入 `t*(x)`。
2. **权利要求 1 与权利要求 14（系统）**：点云关联句同步收紧为"与**取得所述最近距离的** IFC 三角表面匹配，**使每个点至多匹配一个**；并列时按预设确定性规则择一"，杜绝重复计数并消除归属歧义；说明书 3.2 技术方案段同步。

已复核：18 项权利要求编号连续、依赖关系正确，"所述X"的引用基础扫描未发现实质缺失。

---

## 五、两个需要代理人确认的问题

0. **合并式还是拆分式权利要求？**（见 `02_修订版权利要求/权利要求结构两方案对照`，我方建议拆分式）
1. **是否恢复"构件级点数统计"替代路径？** 代理人在 2026-07-22 批注（comment5）中曾指出该特征在原权 6 中缺少引用基础并"请判断是否要纳入权利要求 1"，我方据此纳入；本次两条权利要求中未见。该路径对应实施例 11（真实点云诊断），去掉后实施例 11 将不被权利要求 1 覆盖。
2. **检测与规划是否仍要拆分？** 代理人在 2026-07-22 批注（comment0）中曾建议"可以在权利要求 1 公开缺口检测方法、在从属权利要求中公开补充扫描视点规划方法，这样保护范围更大"；本次 S1–S10 合并在同一独立权利要求中，规划步骤回到了权 1。修订稿**保留了合并结构未擅自拆分**，请确认是否为有意调整。

---

## 六、目录结构

```
01_答复文件/        疑问解答 + 逻辑审查（docx + md）
02_修订版权利要求/  修订版权利要求 1–2 + 逐条对照表（docx + md）
03_我方文件更新/    更新后的说明书及权利要求书、权利要求书单独版
04_验证材料/        verify_s4_association.py（四项几何验证）+ tie_break.py（规则实现）
                    + test_tie_break.py（17项单测）+ measure_real_mesh.py（真实网格测量）
                    + 运行输出/（pytest 与真实网格测量的完整输出）
                    + pointcloud主流程改动.diff（规则接入主流程的实际改动）
05_合作方原始文件/  代理人原件
```
