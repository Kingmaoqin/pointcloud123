你现在作为本项目的“算法研发工程师 + 实验设计负责人 + 专利技术方案审查员”继续工作。

你的任务是针对第二件专利当前版本，解决合作方提出的一个核心质疑：

“当前测试场景给规划器提供了过强的模型几何先验。既然完整模型已经提供足够好的几何信息，为什么还需要补扫？实际场景中更常见的情况可能是只有点云，或者参考模型只覆盖已有点云对应的目标，并不存在一个比点云覆盖范围更大、还准确描述完整现场几何的模型。与此同时，未知环境探索、SLAM exploration、Next-Best-View 等方向已有较多工作。”

本轮目标不是推翻现有专利，也不是把方法彻底改造成无模型 SLAM / frontier exploration。

本轮目标是：

【以尽可能小的算法、代码和专利文本修改，降低当前方案对“完整且准确的现场几何模型”的依赖，同时保留现有“目标表面缺口检测 + 任务驱动补扫”的核心定位。】

==================================================
一、工作总原则
==================================================

1. 严格遵守“最小修改”原则。

除非实际代码证明无法实现，否则：

- 不推翻现有 S1–S8 总体结构；
- 不删除现有缺口证据体系；
- 不删除 G_gap、G_task；
- 不删除候选生成、路径预算、多站集合选择；
- 不删除在线遮挡更新闭环；
- 不重新设计一套纯 SLAM 系统；
- 不重新定义整个发明主题。

我们要解决的是：

“完整现场模型先验过强”

而不是：

“彻底取消所有参考模型”。

--------------------------------------------------

2. 严格区分三类信息。

后续所有分析和修改都必须标明：

[A] 当前代码/实验真实存在；
[B] 根据当前实现可合理推出；
[C] 本轮建议新增；
[D] 当前无法确认，必须先查代码或数据。

不得把[C]写成已经实现。

--------------------------------------------------

3. 第一轮必须审计代码，不得直接按照本 Prompt 假设现有实现。

本 Prompt 提供的是目标方向，不代表当前代码已经具备：

- 点云占据地图；
- unknown/free空间状态；
- frontier detection；
- 增量 occupancy mapping；
- point-cloud-only visibility；
- 部分先验随机删除；
- 在线自由空间扩展。

如果代码没有，必须明确写“当前没有”。

--------------------------------------------------

4. 本轮不以“能够完全无模型运行”作为目标。

当前 S1 的“缺口”定义依赖一个目标参考：

哪里理论上应该存在表面；
表面属于哪个工程构件；
需要达到什么扫描质量；
哪些构件更重要。

如果完全没有任何参考目标，则“这个区域是不是缺口”本身需要重新定义，会转入：

- active mapping；
- SLAM exploration；
- reconstruction uncertainty；
- occupancy/frontier exploration；
- prediction-guided NBV；

这属于另一类技术问题。

因此本申请当前目标应限定为：

“存在目标参考信息，但实际现场环境几何并不完整已知的任务驱动补充扫描。”

==================================================
二、首先重新定义两个模型
==================================================

当前最大的概念问题，是“设计模型”同时承担：

1. 告诉系统需要扫描什么；
2. 告诉系统现场所有几何和遮挡物在哪里。

本轮必须把这两个角色拆开。

--------------------------------------------------
2.1 目标参考模型 M_ref
--------------------------------------------------

定义：

M_ref = Target Reference Model

中文统一称：

“目标参考模型”。

第一次出现时解释：

“目标参考模型，是用于描述本次扫描任务中需要检查或补充采集的目标构件、目标表面及其工程属性的参考信息；其作用是定义‘需要扫描什么’，不要求完整、准确地描述现场全部障碍物、临时物体和非目标环境几何。”

M_ref 可以来源于：

- IFC/BIM；
- CAD；
- 既有数字孪生；
- 竣工模型；
- 资产清单附带局部几何；
- 仅包含目标设备/目标表面的局部模型。

重要：

不得默认 M_ref 等于整个真实现场。

M_ref 可以只覆盖“需要验收/补扫的目标”。

S1主要继续使用 M_ref。

--------------------------------------------------
2.2 规划环境模型 M_plan
--------------------------------------------------

定义：

M_plan = Planning Environment Model

中文统一称：

“规划环境模型”。

第一次出现时解释：

“规划环境模型，是根据当前已经获得的现场观测以及可选的可信安全先验形成的环境几何表示，用于判断移动平台可通行性、候选站位遮挡关系和视线可见性；它表示系统当前‘认为现场是什么样’，并允许随着新增扫描逐轮更新。”

核心区别：

M_ref 回答：

“我要扫什么？”

M_plan 回答：

“我现在认为现场哪些地方被占据、哪些视线会被挡住？”

不得再把二者默认为同一个模型。

==================================================
三、先完成“现有代码先验依赖审计”
==================================================

在任何代码修改之前，先完整检查当前代码。

至少逐项回答：

### 3.1 S1

哪些代码使用完整 IFC/设计模型？

区分：

- 用于目标表面定义；
- 用于点—表面关联；
- 用于缺口证据；
- 用于工程重要度。

这些原则上可以继续使用 M_ref。

--------------------------------------------------

### 3.2 S2

当前可通行空间到底来自什么？

检查：

- 是否直接将整个 IFC 模型投影成障碍；
- 是否把所有 IFC 中的墙/设备都当成现场真实障碍；
- 是否使用初始点云；
- 是否存在 occupancy map；
- 是否区分 free / occupied / unknown；
- unknown 当前是否被默认当成 free。

把所有“完整模型 oracle”位置列出来。

--------------------------------------------------

### 3.3 S3

候选生成中：

- 目标导向候选需要哪些 M_ref 信息？
- 自由空间候选依赖哪些 M_plan 信息？
- 是否存在“IFC中为空 → 自动认为机器人可站”的隐含假设？

--------------------------------------------------

### 3.4 S4

重点检查：

当前 ray casting / visibility 查询使用的几何是什么？

是否实际：

M_plan^(0) = 完整 IFC 三角面集合 T？

如果是，必须标记为本轮需要解决的主要 oracle 来源。

--------------------------------------------------

### 3.5 S7/S8

检查：

新增体素到底是加入：

- IFC/T；
- 一个独立 M_plan；
- 还是临时 BVH？

检查 M_plan 当前是否已经逻辑独立于目标模型。

--------------------------------------------------

完成后输出：

《现有完整模型先验依赖审计》

格式：

| 模块 | 当前使用模型 | 使用目的 | 是否属于过强环境先验 | 是否必须修改 |
|---|---|---|---|---|

不得跳过这一步。

==================================================
四、本轮最小算法修改目标
==================================================

在完成代码审计后，尽量采用下面结构。

--------------------------------------------------
4.1 S1 原则上不改
--------------------------------------------------

S1继续：

M_ref + 已采集点云
→ 表面分块
→ 点—分块关联
→ 缺口证据
→ G_gap
→ G_task
→ 缺口表面分块。

只需要把“设计模型”在概念上明确为 M_ref。

不要因为合作方质疑就取消参考表面。

--------------------------------------------------
4.2 S2 改为使用当前可确认环境信息
--------------------------------------------------

目标：

可通行空间不能再依赖“完整M_ref=完整现场”这一假设。

优先考虑：

M_plan^(k)
+
可选安全先验
→ 可通行空间。

如果当前代码不存在 occupancy/free/unknown 表示，则先设计“最小可实现版本”。

最低要求：

- observed occupied：已观测为占据；
- observed free：已有观测支持为空闲；
- unknown：没有证据证明可通行。

不得直接把 unknown 当成安全 free。

如果完全构建 free-space carving 会造成修改过大，则可以提出最小替代方案，例如：

“只允许在由当前观测确认的可通行区域内生成平台站位。”

具体采取哪种，以现有代码结构和改动量决定。

必须写明 trade-off。

--------------------------------------------------
4.3 S3 尽量保持不变
--------------------------------------------------

候选仍由两部分组成：

A. 目标导向候选：
基于 M_ref 中缺口表面的：

- 形心；
- 法向；
- 距离档；

生成。

B. 环境自由空间候选：
基于当前 M_plan / 已确认自由空间生成。

这非常重要：

本方法不是纯 frontier exploration。

它仍有“任务目标导向”。

--------------------------------------------------
4.4 S4 是本轮最关键修改
--------------------------------------------------

候选站位可见性必须明确写成：

使用当前轮规划环境模型：

M_plan^(k)

而不是默认使用完整目标参考模型 M_ref。

需要检查并修改所有：

- ray casting；
- BVH；
- visibility query；
- first-hit query。

如果目标表面自身必须参加 ray intersection，则允许：

M_plan^(k)
+
当前被评估的目标表面几何

共同用于判定。

但不得重新把所有 M_ref 场景几何无条件加入遮挡模型。

这一点要技术上处理严谨：

“目标本身需要作为命中目标”
≠
“整个参考模型全部作为现场遮挡真值”。

--------------------------------------------------
4.5 S7a 上位化
--------------------------------------------------

原表述：

“发现设计模型外遮挡物”。

改为更一般的：

“发现当前规划环境模型中尚未记录的占据几何”。

具体逻辑可以仍然使用：

新增点云
→ 与当前已有环境/参考目标比较
→ 判定新的占据区域
→ 体素化
→ 加入 M_plan^(k+1)。

但必须重新分析：

当 M_plan 本身不完整时，

d(x, M_ref) > ε

究竟表示：

“模型外障碍”

还是：

“当前未被参考模型描述的环境几何”。

不要继续默认所有不靠近 M_ref 的点都是“异常临时遮挡”。

新的技术语义应当是：

“非目标环境占据几何”。

这些几何：

- 可以挡住后续扫描；
- 不自动成为补扫目标。

--------------------------------------------------
4.6 S8 保持双闭环
--------------------------------------------------

闭环一：

新增有效点云
→ 累计观测
→ 更新缺口证据
→ 更新 G_gap/G_task
→ 更新缺口表面。

闭环二：

新增有效点云
→ 更新 M_plan
→ 下一轮 S2/S4 使用新的现场环境认识。

这里尤其检查：

既然 M_plan 现在也影响可通行空间，

是否应当：

M_plan 更新
→ S2重新更新部分可通行空间

而不是只回灌 S4。

如果代码上可行，应分析是否需要：

S8 → S2/S3/S4

而非仅：

S8 → S4。

这是一个必须认真检查的新数据流问题。

不得机械沿用旧流程图。

==================================================
五、明确与 SLAM / exploration / NBV 的边界
==================================================

本轮必须重新写一份技术定位。

不要说：

“本方法解决未知环境探索。”

正确定位应当是：

“面向具有目标参考信息、但现场环境几何不完整或不完全可信的扫描任务，根据目标表面的扫描质量缺口和工程优先级，在移动与预算约束下进行任务驱动的补充扫描，并利用新增观测在线更新规划环境模型。”

与通用 SLAM / frontier exploration 的核心区别：

### SLAM / exploration

主要目标：

- 扩大地图覆盖；
- 减少未知空间；
- 最大化地图信息增益；
- 建立环境地图。

### 本方法

主要目标：

- 补齐指定目标表面的扫描缺口；
- 达到目标点密度/角度/覆盖质量；
- 优先重要工程构件；
- 在移动预算内完成任务。

即：

exploration：

“哪里不知道，就去哪里看。”

本方法：

“哪些任务目标还没有扫描合格，就去最值得补扫的位置。”

--------------------------------------------------

必须保留：

M_ref 可以是部分目标模型。

不得声称：

M_ref 必须覆盖整个现场。

不得声称：

没有任何目标参考时当前方法仍然成立。

==================================================
六、代码修改必须遵守“最小侵入”
==================================================

完成审计后，先输出：

《最小代码修改方案》

按文件列：

文件：
函数：
当前行为：
问题：
最小修改：
风险：
测试方法：

例如：

src/.../visibility.py
当前：
BVH = full_ifc_mesh

建议：
BVH = planning_environment_mesh + current_target_surfaces

但是：

这只是格式示例。

实际文件名必须以真实仓库为准。

不得凭空发明代码位置。

==================================================
七、新增实验 E7：规划环境先验强度消融
==================================================

这是本轮最重要的新实验。

目的不是重新证明整个算法。

只回答合作方：

“是不是因为给了太完整的几何模型，所以问题太简单？”

--------------------------------------------------
7.1 控制变量
--------------------------------------------------

固定：

- M_ref；
- 初始点云生成方式；
- 缺口定义；
- 扫描预算；
- S1缺口证据；
- 候选生成主要参数；
- 执行策略；
- 随机种子。

只改变：

“规划器在初始时能获得多少环境几何”。

--------------------------------------------------
7.2 至少四档
--------------------------------------------------

P100：

规划环境初始拥有完整环境几何。

这就是现有实验条件，作为上限/理想条件。

P75：

规划初始只拥有约75%的非目标环境几何。

P50：

约50%。

P0：

规划初始不获得额外环境几何；

M_plan仅由初始实际观测建立。

注意：

M_ref 不删除。

目标表面仍然存在。

删除的是“规划器提前知道整个现场怎么遮挡”的能力。

--------------------------------------------------
7.3 几何删除必须设计严谨
--------------------------------------------------

不得简单随机删除三角面，造成：

- 一个实体只剩半张皮；
- 不物理的穿透；
- target surface本身被删掉；
- 人为破坏拓扑导致实验没有意义。

优先按：

object/component/group

为单位控制环境几何先验比例。

例如：

目标构件保留在 M_ref；

非目标环境物体按对象随机选择部分进入 M_plan^(0)。

具体必须根据数据结构决定。

--------------------------------------------------
7.4 E7主要指标
--------------------------------------------------

至少报告：

1. 缺口加权恢复率；
2. 构件等权恢复率；
3. 关键构件召回；
4. 实际移动路径长度；
5. 执行站数；
6. 可见性预测 MAE；
7. 无效视点数量；
8. 在线新增占据体素/环境几何数量；
9. 随轮次 M_plan 可见性预测误差变化；
10. 若有，任务因未知区域不可达而提前结束次数。

--------------------------------------------------
7.5 最重要的曲线
--------------------------------------------------

建议增加：

x轴：执行轮次

y轴：

- visibility MAE；
- 缺口恢复率；
- M_plan 已知环境比例。

这样可以直接说明：

先验较弱时，随着实际扫描增加，规划环境模型是否逐步收敛。

--------------------------------------------------
7.6 统计要求
--------------------------------------------------

优先使用现有实验的随机种子体系。

至少保证每个 prior level 使用同一组 seeds。

比较：

P100 / P75 / P50 / P0

时采用配对统计。

若运行成本过高：

第一阶段先每档3个seed做验证；

确认趋势后再扩展到正式样本规模。

不得用单个seed直接声称稳健性。

==================================================
八、新增一个最小 exploration baseline
==================================================

合作方已经明确提出 SLAM exploration。

因此建议增加一个传统基线，但不要因此实现大型学习模型。

优先：

Frontier-based Exploration。

定义：

frontier = 已确认自由空间与未知空间的边界。

基本策略：

1. 从当前 occupancy/free/unknown map 检测 frontier；
2. 将可达 frontier 聚类；
3. 选择一个 frontier 目标；
4. 使用与本方法相同的路径规划和移动预算；
5. 执行扫描；
6. 更新环境地图；
7. 重复。

--------------------------------------------------

baseline必须保持公平：

相同：

- 平台；
- 传感器；
- 起始站；
- 移动预算；
- 扫描站数上限；
- 障碍安全约束。

差异只在目标函数。

Frontier：

最大化环境探索/未知空间发现。

本方法：

最大化目标表面缺口恢复。

--------------------------------------------------
8.1 对比指标
--------------------------------------------------

重点不是比较谁建图最好。

重点比较：

相同预算下：

- 缺口加权恢复率；
- 关键构件召回；
- 构件等权恢复率；
- 路径；
- 环境探索覆盖率。

预期可以出现：

frontier在环境探索覆盖上更好；

本方法在指定工程表面恢复上更好。

这种结果是合理的。

不要要求本方法在所有指标上胜过frontier。

==================================================
九、现有实验全部保留，不得作废
==================================================

当前：

- 完整方案 vs 五基线；
- E5跨规模/密度；
- 在线遮挡发现；
- 预算回退；
- 构件分组；
- 失败站去重；
- 最短路效率；
- A/B/C执行策略；
- 真实建筑可执行性；

全部保留。

P100解释改为：

“完整环境几何先验条件下的上限/理想规划条件”。

而不是删掉。

新增E7负责：

“环境几何先验不完整时是否仍可执行”。

==================================================
十、真实建筑实验如何最小调整
==================================================

不要假装当前真实建筑已经是 P0。

现有真实建筑实验：

真实 IFC +
真实融合点云 +
后续射线仿真补扫

继续保留为可执行性验证。

如果计算可承受，可以在真实建筑上额外增加：

- P100；
- P50；
- P0；

至少一个seed的探索性结果。

但是：

单栋楼单seed只用于支持“可执行”，不得支持泛化。

==================================================
十一、专利文本修改暂时只做“影响分析”
==================================================

第一阶段不要立刻改权利要求全文。

先输出：

《专利文本影响分析》

逐项判断：

### 发明名称
是否需要调整？

### 技术问题
原来的“设计模型外临时遮挡”是否应上位化为：

“目标参考信息与真实现场环境几何不一致或不完整”。

### S1
哪些词从“设计模型”改为“目标参考模型”。

### S2
需要怎样修改。

### S4
需要怎样修改。

### S7a
需要怎样上位化。

### S8
规划环境模型更新是否还要回灌S2。

### 权利要求1
现有必要技术特征是否仍然成立。

特别判断：

如果现在把 M_ref 和 M_plan 分开，

独立权是否反而更清楚地形成：

目标参考
+
现场环境在线建立
+
任务驱动补扫

这一技术组合。

==================================================
十二、禁止的修改
==================================================

本轮禁止：

1. 把专利直接改成“无模型SLAM方法”。
2. 删除目标参考模型。
3. 声称完全无参考模型也能检测“缺口”。
4. 为了P0强行使用ground-truth geometry做路径规划。
5. 在P0实验中偷偷继续用完整IFC做遮挡计算。
6. 将unknown区域默认全部当作free而不解释。
7. 用完整IFC做碰撞避障，却声称是point-cloud-only planning。
8. 把frontier结果的地图覆盖优势解释成任务缺口优势。
9. 因为新实验结果不理想就修改已有实验结果。
10. 删除现有P100结果。
11. 为迎合合作方而伪造“真实场景通常完全无模型”的绝对论断。
12. 没查代码就声称某功能已存在。

==================================================
十三、强制 oracle leakage 检查
==================================================

E7完成后必须进行一次：

《Oracle Leakage Audit》

逐项回答：

在P0条件下，以下模块有没有偷偷读取完整场景真值：

- S2 obstacle map；
- free-space map；
- S3 candidate validity；
- S4 ray casting；
- path planning；
- collision check；
- visibility；
- frontier baseline；
- scan simulator；
- evaluation metrics。

注意：

scan simulator可以使用ground truth生成“传感器实际会看到什么”，因为它代表真实环境。

但是规划器在做决策之前不能读取该ground truth。

必须严格区分：

Simulator Truth

和

Planner Knowledge。

==================================================
十四、最终判断标准
==================================================

本轮修改成功的最低标准不是：

“P0和P100一样好”。

而是：

1. P0/P50条件下算法仍然可以运行；
2. 不会因为没有完整环境模型直接崩溃；
3. 新观测能够逐步修正 M_plan；
4. 任务恢复率随着在线观测合理提高；
5. 与frontier相比体现“任务驱动”而不是“未知空间驱动”的差别；
6. 完整先验不再是方法成立的必要条件。

如果P0明显差于P100，这是可以接受的。

真正需要回答的是：

“是否依赖完整环境oracle才能工作？”

而不是：

“弱先验是否完全没有性能损失？”

==================================================
十五、执行顺序
==================================================

严格按以下阶段执行。

### Phase 1 — 现状审计

只读代码和现有文件。

输出：

A. 当前 M_ref/M_plan 实际是否分离；
B. 完整模型oracle在哪里进入；
C. 当前是否已有 occupancy/free/unknown map；
D. 最小修改可行性；
E. 会不会影响当前S1–S8主链。

此阶段不要改代码。

--------------------------------------------------

### Phase 2 — 最小修改设计

输出：

- 新数据流；
- M_ref/M_plan定义；
- 每个步骤修改量；
- 需要新增的数据结构；
- E7设计；
- frontier baseline设计；
- 预计代码改动文件。

然后开始编码。

--------------------------------------------------

### Phase 3 — 单元测试

至少增加：

- M_ref不完整环境时S1仍正确；
- M_plan不再自动包含全部M_ref；
- P0下visibility不得读取完整场景；
- 新观测确实加入M_plan；
- 新加入占据几何不会变成target；
- unknown不会未经规则直接当free；
- frontier baseline只使用当前map；
- P100旧结果路径保持兼容。

--------------------------------------------------

### Phase 4 — 小规模E7 smoke test

先运行：

P100/P50/P0
×
至少1–3个seed。

检查：

- 有没有oracle leakage；
- 有没有路径完全不可用；
- M_plan是否实际增长；
- 恢复率是否有合理趋势。

如果失败：

先查逻辑。

不要马上大规模跑实验。

--------------------------------------------------

### Phase 5 — 正式E7

通过smoke test后再完成正式实验。

输出完整：

表格；
统计；
曲线；
运行配置；
commit；
结果路径。

--------------------------------------------------

### Phase 6 — Frontier baseline

实现最小公平基线并运行。

不得为了让本方案赢而给frontier使用更差的路径规划或更小预算。

--------------------------------------------------

### Phase 7 — 专利修改影响

在新代码和实验稳定后，才给出：

- 发明名称是否修改；
- 背景技术；
- 技术问题；
- 技术方案；
- 有益效果；
- S1–S8；
- 权利要求1；
- 从属权；

的具体修改建议。

暂时不要擅自生成最终申请稿。

==================================================
十六、最终必须交付的文件
==================================================

生成：

1. `01_完整模型先验依赖审计.md`

2. `02_Mref_Mplan最小修改设计.md`

3. `03_代码修改与测试记录.md`

4. `04_E7规划先验强度消融.md`

5. `05_Frontier探索基线对照.md`

6. `06_Oracle_Leakage_Audit.md`

7. `07_专利技术方案修改影响分析.md`

8. `08_最终结论与合作方答复建议.md`

==================================================
十七、最终结论必须回答的五个问题
==================================================

最后不要只说“实验做完了”。

必须明确回答：

1. 当前原方案究竟在多大程度上依赖完整模型先验？

2. 修改后，完整环境模型是否仍然是算法运行的必要条件？

3. M_ref到底最少需要提供什么信息？

4. M_plan在P0情况下如何从初始观测建立、又如何逐轮更新？

5. 本方法与SLAM/frontier/NBV探索的核心技术区别到底是什么？

==================================================
十八、合作方真正需要看到的最终技术定位
==================================================

如果实验支持，最终应尽量形成下面这种技术定位，但只有在代码与实验验证后才允许正式采用：

“本方法不要求设计模型完整描述真实现场环境。目标参考模型用于定义需要检查的目标表面及其工程属性；用于移动和可见性预测的规划环境模型则根据当前实际观测建立并在补充扫描过程中持续更新。因此，本方法解决的不是一般未知环境建图问题，而是在目标任务已知、现场环境几何仅部分已知的条件下，利用在线环境更新进行任务驱动的补充扫描规划。”

如果实验不支持这一结论，必须如实报告具体失败原因，不得强行修改措辞掩盖。

==================================================
十九、现在开始
==================================================

现在先执行 Phase 1。

不要改任何代码。

首先完整检查：

- 当前第二件专利代码；
- 当前S1–S8实现；
- visibility/raycast；
- occupancy/free-space；
- path planning；
- initial planning model；
- online occluder update；
- 当前实验脚本。

然后只输出：

《01_完整模型先验依赖审计》

以及一句明确结论：

“按照当前代码，要实现目标参考模型M_ref与规划环境模型M_plan解耦，属于：
A. 极小修改；
B. 中等修改；
C. 需要重构。

并逐条说明依据。”

完成Phase 1后，直接继续Phase 2–7；只有在发现会改变本发明根本定义、且无法通过现有材料解决的技术事实时，才停止询问我。