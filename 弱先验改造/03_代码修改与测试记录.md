# 03 代码修改与测试记录（Phase 3）

---

## 一、提交序列

| commit | 内容 |
|---|---|
| `960ca9a` | 解耦 M_ref 与 M_plan：新增 `PlanningEnvGrid`，S8 增加占据侧回灌；一并落盘 E2/E5/E6 最终结果 |
| `1072ddb` | 规划器不再绕开设计模型中不存在的临时占位物（`plan_grid`）；新增 E7 runner 与逐站可见性审计 |
| `d41109a` | 新增 frontier 探索基线（首版 0 站，已修） |
| （本次） | 目标集与 M_ref 口径统一；补齐 Phase 3 要求的 8 类测试 |

---

## 二、修改清单

### 新增文件

- `src/patent_gap/mapping/planning_env.py` — `PlanningEnvGrid`（三态观测驱动栅格）
- `scripts/run_e7_prior.py` — E7 先验强度消融
- `scripts/run_e8_frontier.py` — E8 frontier 对照
- `tests/test_weak_prior.py` — 本轮全部不变量

### 修改文件

| 文件 | 修改 |
|---|---|
| `simulation/scene_gen.py` | `Component.is_target`；`ENVIRONMENT_CLASSES`；`SceneModel.prior_tri_mask()`；`build_trav_grid(bim_only=)` |
| `simulation/scene_patches.py` | 非目标环境构件不再进入目标分块表 |
| `simulation/closed_loop_v2.py` | `planning_map()`；`SimWorld.env / ref_tris / plan_grid`；`build(env_prior_frac, observed_env, prior_seed)`；S2 与 S4 双回灌；`EpisodeConfig.vis_audit`；`vis_audit_at()`；`Bfrontier` |

**未改动**：`gap/`、`viewpoints/`、`planning/`、`registration/`、`occlusion/raycast.py`、
`occlusion/sampling.py`。公式 (26)–(45) 的实现本身一行未动。

---

## 三、Phase 3 要求的八类测试

| 指导要求 | 测试 | 结果 |
|---|---|---|
| M_ref 不完整环境时 S1 仍正确 | `test_s1_is_unaffected_by_how_much_environment_the_model_describes` | ✅ |
| M_plan 不再自动包含全部 M_ref | `test_weak_prior_planner_cannot_see_unobserved_environment` | ✅ |
| P0 下 visibility 不得读取完整场景 | 同上 ＋ `test_every_target_surface_is_present_in_the_planning_model` | ✅ |
| 新观测确实加入 M_plan | `test_new_observation_grows_the_planning_map` | ✅ |
| 新加入占据几何不会变成 target | `test_discovered_geometry_never_becomes_a_scan_target`、`test_non_target_environment_is_not_a_scan_target` | ✅ |
| unknown 不会未经规则直接当 free | `test_unknown_is_not_free_by_default` | ✅ |
| frontier baseline 只使用当前 map | `test_frontier_baseline_reads_only_the_observed_map` | ✅ |
| P100 旧结果路径保持兼容 | `test_full_prior_is_bit_identical_to_previous_behaviour` | ✅ |

另加四项本轮发现的问题所对应的回归锁：

- `test_planner_traversability_excludes_geometry_absent_from_the_model`
- `test_frontier_baseline_actually_moves_and_grows_the_budget`（探索基线必须真能用掉预算）
- `test_frontier_baseline_requires_an_observation_driven_map`
- `test_vis_audit_is_off_by_default_and_records_when_on`

---

## 四、开发过程中被查出并修掉的三个缺陷

### 4.1 M_plan 的更新被陈旧缓存吞掉

`TravGrid._compute_free` 缓存 `self._free`，只在其为 `None` 时重算。`PlanningEnvGrid`
改写了被包装栅格的 `_obstacle` 却未让缓存失效，**每一轮新观测对可通行空间的修正都被
丢弃**。表现为 P0 的 awc = 0.043 —— 一个安静的低分，看上去像"弱先验下方法不 work"
这个**结论**，实际上是个**缺陷**。修复后同条件 awc = 0.725。

### 4.2 frontier 基线一站也走不出去

首版把 frontier 栅格本身当作导航目标，结果每个 episode 都是 0 站。原因：未知区按障碍
处理，而 frontier 的定义就是紧贴未知区，按平台半径膨胀后**实测 971 个 frontier 栅格里
可通行的是 0 个**。这是实现把基线卡死了，不是 frontier 方法弱。改为投影到邻近可站
位置（标准做法：驶向 frontier *附近*，不是驶向边界格本身）。

> 一个走不动的基线不构成对照。若照此报出去，等于用一个被自己实现压垮的对手来证明
> 本方法的优越性。

### 4.3 目标集与 M_ref 口径不一致

见 `06_Oracle_Leakage_Audit.md` 第 2.4 节。

---

## 五、两项被放松的既有测试断言

两项断言在目标集口径统一后不再成立。**均已核实为"断言强于算法保证"，而非本轮改动
引入的回归**，且核实过程记录如下。

### 5.1 `test_tight_budget_degrades_instead_of_collapsing`

原断言：恢复率随预算**严格**单调不减。

实测（S/low seed0，B10_full）：

| 预算 | 新口径 awc | 旧口径 awc |
|---|---|---|
| 12 m | 0.064 | 0.186 |
| 25 m | 0.131 | 0.820 |
| 60 m | **0.122** | 0.825 |
| 400 m | 0.996 | 0.991 |

新口径在 25 m→60 m 之间有一次 **0.009** 的反转。

**判定**：该断言此前成立，是因为目标集里含厂房与围墙 —— 面积大、位于边缘、两站就能
覆盖的分块，把小预算档整个抬了起来（25 m 档 0.82 对 0.13）。剔除后剩下的设备才是真正
难扫的。**每轮重规划的性价比贪心在纯路径预算下本就不保证严格单调**：预算放宽会改变
首站选择，截断视野内的收益可能反而略低。

**处理**：改为锁本修复真正针对的性质 —— 预算收紧时逐级退化而非坍塌成零，宽预算显著
优于紧预算（差值 > 0.3），局部反转限定在 0.02 容差内。

### 5.2 `test_failed_registration_does_not_trap_the_planner`

原断言：`n_reg_failed == 1`。用例注入**一次**强制失败，之后交回真实配准判定；口径变更
后站间重叠下降，真实配准又失败了一次，于是变成 2。

**判定**：该用例要锁的是失败之后的控制流（不得原地重选、任务不得因此归零），失败**次数**
取决于站间重叠，而重叠随目标集与采样密度变动，不该被写死。用例中真正表达该性质的两条
断言 `len(set(picks)) == len(picks)` 与 `awc > 0.5` **均通过**。

**处理**：改为 `n_reg_failed >= 1`，保留另两条不变。

---

## 六、全量回归

`python -m pytest tests/ -q` — 见本文件末尾"最终状态"一节。

---

## 七、实验产物的版本可追溯性

E7 与 E8 的每个 `run.json` 记录 `git_commit`，续跑时若与当前不符即 `SystemExit`
拒绝 —— 该守卫是此前三次"版本混合结果集"事故之后加的，本轮同样生效。

需要说明的一点：实验运行期间 `results/*.log` 属被跟踪文件且正在被写入，故
`git status --porcelain` 非空，`git_commit` 记为 `<hash>-dirty`。该后缀来自日志文件
而非源码。为此在实验结束后另做一次**复现校验**：在最终 commit 上重跑若干格，与盘上
结果逐位比对。结果记于第八节。

---

## 八、最终状态

（在 E7/E8 跑完并完成复现校验后填写。）
