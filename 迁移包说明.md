# 迁移包 · pointcloud123

建筑点云缺口检测与补充扫描规划。本包收录**两件专利的定型版本**、**全部算法代码**、
**全部实验记录**与**复现指导**，压缩后约 8 MB，可直接迁移到新机器。

---

## 一、包内有什么

```
01_算法代码/                    1.2 MB   ← 只要代码的话，下载这一个目录就够
    src/  scripts/  tests/  configs/
    environment.yml  requirements.txt  pyproject.toml
    算法指导.md                          ← 算法说明：S1–S8、公式与代码对应、参数表

02_专利定稿/                    5.4 MB
    专利一_点云缺口补扫视点规划/
        A_初稿_早版本/                   ← 早版本
        B_修改稿_定型版本/               ← 对照代码修改后的定型稿
        C_审查与代码核实/                ← 修改依据（逐项 文件:行号）
    专利二_设计模型与在线遮挡发现/
        A_送审稿/                        ← 权利要求书、说明书、六幅附图、待确认事项
        B_规划环境先验解耦_最终交付/     ← 14 份 FINAL 文件 + 附图 + 实验记录

03_数据与实验/                  1.8 MB
    数据获取指南.md                      ← 合成数据怎么生成、CRAS 怎么下载
    实验使用方法.md                      ← 怎么跑、怎么读结果、四条运行纪律
    结果摘要/                            ← 各实验汇总表 + 四张曲线（480 KB）
    完整实验原始记录_results.tar.gz      ← 1412 次运行的原始 run.json（1.3 MB / 解压 17 MB）
```

---

## 二、按需要看哪几份

| 你要做的事 | 看这些 |
|---|---|
| **只要跑代码** | `01_算法代码/`，先读 `算法指导.md`，再读 `03_数据与实验/实验使用方法.md` |
| **交给专利代理人** | `02_专利定稿/`，两件各自的定型版本 |
| **复核实验数字** | `03_数据与实验/结果摘要/` ＋ 解压原始记录，跑 `check_delivery_numbers.py` |
| **迁移到新机器** | 见第四节 |

---

## 三、两件专利的关系

| | 专利一 | 专利二 |
|---|---|---|
| 名称 | 建筑点云缺口补扫视点规划方法、装置、设备及存储介质 | 基于设计模型与在线遮挡发现的补充扫描规划方法及系统 |
| 解决什么 | **缺口在哪** —— 输入模型与点云，输出单个补充扫描视点 | 现场存在设计模型中不存在的遮挡物，导致可见性被系统性单向高估且逐轮重复；多站补扫在预算收紧时提前终止 |
| 是否涉及移动平台 | 否 | 是（可通行性、多站组织、路径预算） |
| 本包内的版本 | 初稿（早版本）＋ 修改稿（定型） | 送审稿 ＋ 规划环境先验解耦最终交付 |

专利一中"视点价值如何算得更准"的部分，在性质上是专利二相应步骤的上位；两件的边界
与关联申请风险见 `专利二/A_送审稿/03_补充材料/待确认事项`。

---

## 四、迁移到新机器

```bash
# 1) 解包
tar xzf 迁移包_pointcloud123.tar.gz && cd 迁移包_pointcloud123

# 2) 建环境
cd 01_算法代码
conda env create -f environment.yml && conda activate patent_gap
#   或 pip install -r requirements.txt

# 3) 验证环境
pytest tests/ -q                      # 应为 128 passed

# 4) 还原实验记录（可选）
tar xzf ../03_数据与实验/完整实验原始记录_results.tar.gz

# 5) 跑主实验（不需要任何外部数据）
python scripts/run_e7_prior.py --quick --out results/exp7_smoke    # 先冒烟
python scripts/run_e7_prior.py --out results/exp7                  # 正式, 约 3–4 h
```

> **ortools 必须锁 9.10**，升级会改变开环 TSP 的解，使既有结果不可复现。

---

## 五、数据：主要实验零下载

**E2 / E3b / E5 / E7 / E8 共 696 次运行不需要下载任何外部数据** —— 场景由
`simulation/scene_gen.py` 按种子生成，同一种子必然复现同一结果（实测重跑三格逐位一致）。

只有真实建筑实施例 **E6** 需要 CRAS 数据集（4.0 GB 点云 ＋ 65 MB IFC），
**未随包分发**，下载方法见 `03_数据与实验/数据获取指南.md`。

---

## 六、五条必须随数字一起交代的边界

引用本包任何实验数字时，这几条要一并说明（完整 7 条见
`专利二/B_.../10_最终交付一致性检查_FINAL`）：

1. **初始站位是作业输入**，不由本方法规划；本方法不解决"完全无信息时如何架第一站"。
2. 实验属于**已有初始扫描之后的补扫**：P0 下仅初始三站后已确认约 89% 的二维栅格，
   **不用于证明从完全未知环境开始的自主探索**。
3. **E8 只直接实测 frontier-based exploration**，不是 NBV / SLAM 基准测试。
4. 合成变电站中可移除的非目标环境按三角面计仅占 **3.6–5.3%**，代价数字**不得外推**。
5. **E7/E8 与更早的 E2/E5/E6 绝对值不可跨实验比较**（目标集口径已统一）；
   各实验内部的对照不受影响。

另有一份**缺陷记录**在 `结果摘要/缺陷记录_勿引用/`，那是修复前的运行，
**不得引用为性能数据**。

---

## 七、可专利性提示

截至本包建立时，两件专利的申请状态**均未确认**（母专利的申请日与公开日仍待查；
专利二的最终申请稿尚未落笔）。**未申请的专利材料一经公开发表，即构成对抗自己的现有
技术**。相应的 GitHub 仓库 `pointcloud123` 已建为**私有**，公开前请先确认申请日。
